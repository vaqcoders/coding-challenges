// Instructions
// * change the ai.js file's name to something else
// * change this file's name to 'ai.js'
// * edit the <a> tag in index.html to add your name and github profile
// * open index.html in your prefered browser; e.g. Chrome
// * refresh the page in chrome to run your program
// * ctrl+shift+i in chrome to open the Developer Console
// * use the Developer Console to debug and find errors
// * ask ethan.r.davidson@gmail.com for help or to ask questions
// * happy programming

class AI {

  static locateNearestFood(snake, foods) {
    const x1 = snake.tail[0].pos.x,
          y1 = snake.tail[0].pos.y;
    // return a Segment instance from
    // foods.data that you would like
    // the snake to advance towards.
  }

  static getOptimumVel(snake, segment) {
    // const possibleReturnValues = [
    //   {x: 1, y: 0}, {x: -1, y: 0},
    //   {x: 0, y: 1}, {x: 0, y: -1}
    // ];
  }

}
